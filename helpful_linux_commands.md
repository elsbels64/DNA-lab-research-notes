# grep
pull out every line of a file that matches a regular expression</br>
grep [options] regex_pattern [file...]</br>
ex.: grep "test1" test_output_cp.seg > test1_output.seg</br>
--> pulls out every line with "test1" in test_output_cp.seg and sends the output to test1_output.seg</br>
