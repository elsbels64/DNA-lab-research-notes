# grep
pull out every line of a file that matches a regular expression</br>
`grep [options] [regex_pattern] [file...]`</br>
example: `grep "test1" test_output_cp.seg > test1_output.seg` --> pulls out every line with "test1" in test_output_cp.seg and sends the output to test1_output.seg</br>
